%h1 ChannelBack Categories
- @categories.each do |category|
  %p= category.name
  = link_to 'Edit',edit_category_path(category)
  = link_to 'Delete',category_path(category),:method => :delete
  %br
%br
= link_to 'New Category',new_category_path